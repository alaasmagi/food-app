# Base URL of the backend API, under the same parent domain as this app.
# Login/logout are full-page navigations to <base>/account/... and the token
# exchange calls <base>/api/v1/account/token.
VITE_API_BASE_URL=https://api.<domain>
